// ES5
function square(num){
    return num * num;
}

// ES6
const squareArrow = (num) => {
    return num * num; // Explicit return
}

// ES6 - Shorter
const squareArrow2 = num => num * num; // Implicit return